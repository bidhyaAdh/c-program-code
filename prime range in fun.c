//wap to display all the prime numbers betn the range enter by the user ;

#include<stdio.h>
#include<conio.h>
int prime (int,int);

void main()
{
	int n1,n2,c,j,p;
	printf("enter a range");
	scanf("%d%d",&n1,&n2);
	p=prime(n1,n2);
		if(c==2)
		{
			printf("%d]/t",j)
     	}
}
	
	getch();	
}
int prime ("int e, int d")
{
	int i;
	for (j=e; i<=j;i++)
	{
		if ( j%i==0)
		{
			c=c+1;
		}
	}
	return prime;
}

