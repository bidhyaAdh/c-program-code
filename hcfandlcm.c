#include<stdio.h>
#include<conio.h>
int hcf(inta, intb);
int lcm(inta, intb);
void main()
{
	int s,a,b,min;
	printf("enter a and b:");
	scanf("%d%d",&a,&b);
	s=hcf(a,b);
	lcm=(a*b)/hcf
	if(a>b)
	{
		min=b
	}
	else
	{
		min=a
	}	
		lcm=(a*b)/hcf
		printf("lcm is%d=")
	getch();
}
int hcf( int c, int d)
{
 if( a%min==0 && b%min==0)
 {
 	hcf=min;
 }
 else
 {
 	min=min-1;
 }
 return(lcm)
}

